# site.yml

**Last Updated:** 2026-02-08 14:20:09

## Overview
This playbook contains 2 play(s).

### Play 1: Configure Linux Guests
**Target Hosts:** `linux_guests`

**Tasks:**
- Ensure system is updated
- Install essential packages
- Configure SSH Daemon

### Play 2: Configure Proxmox Nodes
**Target Hosts:** `proxmox_nodes`

**Tasks:**
- Ensure Proxmox packages are updated (safe update)

## Usage

```bash
# Unlock Vaultwarden (if using secrets)
source unlock_vault.sh

# Run the playbook
ansible-playbook -i inventory/hosts.ini playbooks/site.yml --check  # Dry run
ansible-playbook -i inventory/hosts.ini playbooks/site.yml        # Execute
```

## Security Notes
- Passwords are stored in Vaultwarden
- SSH host key checking can be disabled for first run with `ANSIBLE_HOST_KEY_CHECKING=False`
- Always test with `--check` first

---
*Auto-generated by Ansible Documentation Agent*
