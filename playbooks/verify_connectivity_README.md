# verify_connectivity.yml

**Last Updated:** 2026-02-08 14:32:33

## Overview
This playbook contains 2 play(s).

### Play 1: Verify Local Environment and Vaultwarden
**Target Hosts:** `localhost`

**Tasks:**
- Check if Bitwarden (bw) is installed
- Fail if bw is missing
- Check Bitwarden Login Status
- Verify Vaultwarden Session
- Test Secret Retrieval (Optional - requires item 'proxmox_pve1_root')

### Play 2: Verify Proxmox Connectivity
**Target Hosts:** `proxmox_nodes`

**Tasks:**
- Ping Proxmox Node

## Usage

```bash
# Unlock Vaultwarden (if using secrets)
source unlock_vault.sh

# Run the playbook
ansible-playbook -i inventory/hosts.ini playbooks/verify_connectivity.yml --check  # Dry run
ansible-playbook -i inventory/hosts.ini playbooks/verify_connectivity.yml        # Execute
```

## Security Notes
- Passwords are stored in Vaultwarden
- SSH host key checking can be disabled for first run with `ANSIBLE_HOST_KEY_CHECKING=False`
- Always test with `--check` first

---
*Auto-generated by Ansible Documentation Agent*
