# debug_vault.yml

**Last Updated:** 2026-02-08 14:20:09

## Overview
This playbook contains 1 play(s).

### Play 1: Debug Vault Lookup
**Target Hosts:** `localhost`

**Tasks:**
- Try to lookup the password
- List all items matching search term (raw JSON)
- Show matching items
- Suggestion

## Usage

```bash
# Unlock Vaultwarden (if using secrets)
source unlock_vault.sh

# Run the playbook
ansible-playbook -i inventory/hosts.ini playbooks/debug_vault.yml --check  # Dry run
ansible-playbook -i inventory/hosts.ini playbooks/debug_vault.yml        # Execute
```

## Security Notes
- Passwords are stored in Vaultwarden
- SSH host key checking can be disabled for first run with `ANSIBLE_HOST_KEY_CHECKING=False`
- Always test with `--check` first

---
*Auto-generated by Ansible Documentation Agent*
